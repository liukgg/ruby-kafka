---

machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install --upgrade docker-compose==1.5.2
    - bundle

test:
  pre:
    - docker-compose pull
    - docker-compose up -d
  override:
    - bundle exec rspec
  post:
    - docker-compose stop
    - docker-compose rm
